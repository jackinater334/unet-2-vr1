using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class primitave : MonoBehaviour
{
    int hp = 10;
    Rigidbody2D a = null;
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(hp);
        Debug.Log(a);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
